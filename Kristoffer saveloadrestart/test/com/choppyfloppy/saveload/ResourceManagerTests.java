package com.choppyfloppy.saveload;

import org.junit.Test;

import java.io.IOException;

import static javafx.scene.input.KeyCode.T;
import static org.junit.Assert.*;

public class ResourceManagerTests {
/*
    @Test(expected = NullPointerException.class)
    public void saveGameDataCanNotBeNull() {
        ResourceManager resourceManager = new ResourceManager();
        resourceManager.saveGame(null, new String);
    }

    @Test(expected = NullPointerException.class)
    public void saveGameStringCanNotBeNull() throws IOException {
        ResourceManager resourceManager = new ResourceManager();
        resourceManager.saveGame(new Class<T>(), null);
    }

    @Test(expected = NullPointerException.class)
    public void loadGameCanNotBeNull() {
        ResourceManager resourceManager = new ResourceManager();
        resourceManager.loadGame(null, new Class<T>());
    }
    */
}